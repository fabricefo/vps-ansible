- name: Common provisioning (both VPS)
  hosts: vps
  become: true
  roles:
    - user
    - packages
    - ssh
    - iptables
    - fail2ban
    - docker

- name: Install Caddy only on VPS1
  hosts: vps1
  become: true
  roles:
    - caddy
